server:
  port: 9001

spring:
  application:
    name: cart-consumer
  resources:
    static-locations: classpath:/statis/
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password@localhost:8761/eureka

logging:
  level:
    root: INFO